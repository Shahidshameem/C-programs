// Programe in c to compare three distinct integers.
#include <stdio.h>

int main() {
    int a,b,c;
    printf("please enter the number");
    scanf("%d%d%d",&a,&b,&c);
    if (a>b,a>c)
    {
      printf("%d is greatest",a);}
      else if (b>c,b>a)
   {  printf("%d is greatest",b);
    }
    return 0;
}
